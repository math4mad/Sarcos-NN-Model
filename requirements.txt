# Pinned to what was resolved and verified on this host
# (macOS 26.1 arm64, CPython 3.14.7). torch 2.14.0 ships cp314 macosx_14_0_arm64 wheels.
#
# Study core only. For the Quarto presentation note see requirements-note.txt
# (or `pip install -e ".[note]"`) - pandas/great_tables/jupyter are not needed
# to train, truncate or report.
numpy==2.5.3
scipy==1.18.1
torch==2.14.0
matplotlib==3.11.1
