# data is licensed via the GPML book and must not be redistributed
data/
results/

# environment
.venv/
__pycache__/
*.pyc
*.egg-info/

# macOS clutter
.DS_Store

# rendered output: the site lives on the gh-pages branch, not next to the code
docs/
.quarto/
**/*.quarto_ipynb
notes/results.html

# scratch dir used by sarcos_svd.publish to stage the gh-pages branch
.gh-pages-worktree/

/.quarto/
